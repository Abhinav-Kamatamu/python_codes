import numpy as mp

class NeuralNetwork:
    def __init__(self, size):
        # size = [a, b, c, etc] # where a, b ,c would be the number of neurons on each layer:
    def createNetwork(self):
        # This function should create individual Layer classes for each of the layers defined by the size list.
        # These Layers should be stored in like a list or something
    # Each layer should have it's own randomised weights and biases
    def ReLu(self,X):
        # Outputs the ReLu activation of the inputed value.
    def softmax(self, X):
        # Return the softmaxed values of the output layer

    class Layer:
        def __init__(self, n_l):